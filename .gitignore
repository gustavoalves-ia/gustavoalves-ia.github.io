# --- conteúdo privado / interno (não sobe pro repo público) ---

# rascunhos em markdown (fonte privada antes do publish)
drafts/

# skills internas (ferramentas, não conteúdo) — agrupadas em skills/
skills/

# doc interno do hub
_playbook.md

# brief + bundle do claude design (histórico, não-público) — reference já coberto por skills/
**/handoff/

# testes / legacy local (posts antigos que não vão pro ar)
testes/

# --- sistema ---
.DS_Store
Thumbs.db
*.log

# --- editores ---
.vscode/
.idea/
*.swp
*~

# --- secrets ---
.env
.env.*
!.env.example
